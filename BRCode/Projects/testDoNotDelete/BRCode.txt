setup {
    fileName = "testDoNotDelete"
    fileDescription = ""
    codeVersion = "A1"
    centerPosition = [0, 0, 0]
    centerRotation = [90, 0, 0]
}

import if
import fpsSensor

myswitch = Switch(inputScale = [0, 1, 1, -1])

newswitch = Switch()

if (myswitch > 0) {
    return(newswitch, 1)
}